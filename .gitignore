# External defect corpus — fetched by scripts/fetch-corpus.sh, never vendored
corpus/

# Dependencies. There are none by design; this guards against a stray install.
node_modules/
package-lock.json

# Local run artifacts
*.sarif
.crosscheck-baseline.json

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/

# Result cache — content-addressed, safe to delete
.crosscheck/cache/

# Meta-output: PRDs, workplans, handoffs, scratch scripts
scratchpad/
